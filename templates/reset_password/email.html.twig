<h1>Réinitialiser votre mot de passe</h1>

<p>Pour réinitialiser votre mot de passe, veuillez visiter le lien suivant</p>

<a href="{{ url('app_reset_password', { token: resetToken.token }) }}">{{ url('app_reset_password', { token: resetToken.token }) }}</a>

<p>Le lien expire dans{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Si cette demande ne vient pas de votre part, merci de ne pas prendre compte de ce mail.</p>

<p>Cordialement</p>
