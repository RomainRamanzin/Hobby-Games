{% extends 'base.html.twig' %}

{% block title %}
	HobbyGames | Administration éditeurs
{% endblock %}

{% block body %}
	<link href="/asset/css/admin/utilisateurs.css" rel="stylesheet"/>
	<div class="container my-5">
		<div class="container">
			<div class="glass">
				<div class="return-before-page">
					<a class="return-before-link" href="{{ path('app_admin_editeurs') }}">
						<i class="fa-solid fa-newspaper"></i>éditeurs</a>
					<label class="return-slash">/</label>
					<label class="return-before">Editeur n°
						{{ editor.getId }}</label>
				</div>
				<div class="col-md-12">
					<div class="box-top-admin">
						<h1 class="title-admin">Editeur n°
							{{ editor.getId }}</h1>
					</div>
					{# afficher toutes les infos de l'éditeur #}
					<div class="data-info">

						<div class="data-element">
							<label class="data-label">Nom :
							</label>
							<label class="data-value">{{ editor.getName }}</label>
						</div>

						<div class="data-actions">
							<a class="btn btn-primary" href="{{ path('app_admin_editeur_edit', {'id': editor.getId}) }}">
								<i class="fa-solid fa-pencil"></i>Modifier</a>
							<a class="btn btn-danger" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?')" href="{{ path('app_admin_editeur_delete', {'id': editor.getId}) }}">
								<i class="fa-solid fa-trash"></i>Supprimer</a>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
